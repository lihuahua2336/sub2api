# Sub2API

Sub2API 向用户提供模型访问能力，并管理对应的访问密钥、分组权限和使用额度。

## Language

**本地用户**：
在 Sub2API 完成注册、持有本地权限和 API Key 的用户。生态应用访问资源时，以关联的本地用户确定资源归属。

**Logto 身份**：
由可信 Logto 身份域确认的用户身份，可关联到一个本地用户。同一身份域中的同一用户可通过多个生态应用访问自己获准使用的资源。

**生态应用**：
通过 Logto 用户授权访问 Sub2API 资源的可信自有应用。首版获得密钥读取权限的生态应用，可以读取该用户全部启用的 API Key。

**API Key**：
由 Sub2API 管理、用于访问模型服务的用户密钥，其使用仍受本地权限和计费规则约束。
_Avoid_: 将其与 Logto access token 混称为同一种令牌。

**生态适配层**：
面向生态应用的资源读取入口，将已验证的 Logto 身份关联到本地用户。首版要求用户事先完成本站注册或身份绑定。
